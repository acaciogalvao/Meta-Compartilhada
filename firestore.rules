rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /goals/{goalId} {
      allow read, write: if true;
    }
    match /settings/{document=**} {
      allow read, write: if true; // For prototype purposes. In production, secure this with Firebase Auth.
    }
  }
}
